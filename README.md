# Excel-Analytics-Platform

## 📦 Setup

```bash
# Install backend dependencies
cd backend
npm install

# Install frontend dependencies
cd ../frontend
npm install


📌 Usage
Run backend: npm run dev (inside /backend)

Run frontend: npm start (inside /frontend)

Upload Excel files from the UI and see results instantly.
